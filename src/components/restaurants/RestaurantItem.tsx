import { View, Text } from "react-native";
import React, { FC } from "react";

interface IProps {}

const RestaurantItem: FC<IProps> = (props) => {
  return (
    <View>
      <Text>RestaurantItem</Text>
    </View>
  );
};

export default RestaurantItem;
